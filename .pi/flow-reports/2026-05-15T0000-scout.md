---
# Flow Report: scout — 2026-05-15T0000

**Status:** complete
**Date:** 2026-05-15

## Summary

Comprehensive map of pi-agent-flow: 38 source files, 6 bundled flows, fork-based delegation with sanitized session snapshots, CI/CD via GitHub Actions. Identified circular imports, unclamped maxDepth, deprecated exports, and potential stale test files.

## Files

| File | Role | Size |
|------|------|------|
| `src/index.ts` | Entry point | 466 lines |
| `src/agents.ts` | Agent discovery | 288 lines |
| `src/flow.ts` | Fork runner | 875 lines |
| `src/executor.ts` | Orchestrator | 457 lines |
| `src/snapshot.ts` | Session sanitization | 686 lines |
| `src/config.ts` | Configuration | 389 lines |
| `src/types.ts` | Type definitions | 331 lines |
| `src/transitions.ts` | Transition logic | 85 lines |
| `agents/scout.md` | Scout flow definition | — |
| `agents/build.md` | Build flow definition | — |

## Actions

| # | Action | Status |
|---|--------|--------|
| 1 | Surveyed root directory structure | complete |
| 2 | Inspected core source files (src/*.ts) | complete |
| 3 | Inspected all 6 bundled agent definitions | complete |
| 4 | Inspected CI/CD workflows (.github/workflows/) | complete |
| 5 | Inspected documentation (CLAUDE.md, README.md) | complete |
| 6 | Traced circular import cycle (index.ts ↔ types.ts) | complete |

## Commands

- `ls`
- `find *.ts`
- `find *.md`
- `find *.json`
- Batch reads of multiple file groups

## Not Done

- Read remaining truncated portions of large files (e.g. snapshot.ts, flow.ts)
- Verify stale test files flagged in repo
- Check for `.pi/agents/` in parent paths (user home, workspace root)

## Next Steps

1. Address circular imports in `src/index.ts` ↔ `src/types.ts`
2. Clamp `maxDepth` in flow definitions and runtime
3. Remove deprecated exports from `src/index.ts`
4. Verify and clean up stale test files
5. Delegate fixes to `build` or `audit` flow

## Reasoning

1. The 4-part prompt structure (activation → directive → mission → tools) is the core innovation of this project.
2. Tool optimization is the default path (enabled unless opted out), suggesting performance is a first-class concern.
3. Fork-based isolation is robust but could hit argv/env length limits with very large session snapshots.
4. Session sanitization is thorough — snapshots are scrubbed before being passed to child processes.

## Notes

- Package version: **v1.8.39**
- Peer dependencies on Pi ecosystem packages
- No project-local `.pi/agents/` directory found (only bundled flows used)
- CI/CD is fully automated via GitHub Actions; no local publishing permitted
- Makefile duplicates many `package.json` scripts — potential cleanup opportunity
